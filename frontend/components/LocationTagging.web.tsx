import React from "react";
import { View } from "react-native";
import { ThemedText } from "./themed-text";

export function LocationTagging() {
  return (
    <View>
      <ThemedText>Location tagging is not supported on web</ThemedText>
    </View>
  );
}
